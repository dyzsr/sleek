(ocamllex
 (modules lexer))

(menhir
 (modules parser)
 (flags --explain --dump))

(executable
 (name main)
 (modules
  main
  lexer
  parser
  syntax
  ast
  signals
  inference
  proofctx
  checker
  verifier
  utils)
 (libraries z3))

(env
 (dev
  (flags (:standard))))
