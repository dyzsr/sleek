(ocamllex
 (modules lexer))

(menhir
 (modules parser)
 (flags --explain --dump))

(executable
 (name main)
 (modules main lexer parser syntax ast signals inference proofctx verifier))

(env
 (dev
  (flags (:standard))))
