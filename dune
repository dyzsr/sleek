(ocamllex
 (modules lexer))

(menhir
 (modules parser)
 (flags --explain --dump))

(executable
 (name main)
 (modules
  main
  lexer
  parser
  syntax
  ast
  signals
  inference
  proofctx
  solver
  verifier))

(env
 (dev
  (flags (:standard))))
